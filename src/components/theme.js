import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      background: {
        red: '#b6433e',
        blue: '#041c45',
        pink: '#ecac8e',
        gray: '#98b7d0'
      },
      text: {
        orange: '#d06f4c',
        gray: '#3c5464',
      },
    },
  });

export default theme
